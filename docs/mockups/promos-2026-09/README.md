# /promos 資訊層級改版提案（2026-09-16 起，最新＝v5 可互動原型）

> 一次性的提案資料夾，不是制度檔。方案定案、實作完成後可整包刪除
> （刪之前把「定案內容」寫進 `docs/project/data-pipeline.md` 第 9 節與 `docs/project/history.md`）。

## 這裡有什麼

| 檔案 | 內容 |
|---|---|
| `interactive.html` | **最新（v3）：可互動原型**。22 組卡片全部渲染，可以真的點展開／收合。已發布版本 <https://claude.ai/artifact/2EmQbDavVksqHyTBKVZK1N> |
| `mockups.html` | v2 的靜態比較頁：4 個版型 ＋ 3 個「卡片特色」方案。已發布版本 <https://claude.ai/artifact/6xHgQRGMMagYVdaKXLfPGr> |
| `measure-card-names.js` | 量測卡名在「卡圖下方一行」放不放得下（要先跑 `build-data.js`） |
| `build-data.js` | **提案「方案甲」的參考實作**：算出每張卡的「卡片特色」清單與新戶活動分組。`node docs/mockups/promos-2026-09/build-data.js` |
| `probe-highlights.js` | 第一輪的取證腳本：純照回饋率排序時會挑出什麼（留著當反例） |

兩支腳本都照 `tools/lib/merchant-cards.js` 的成例把 `js/` 載進 Node 的 vm，
用主站自己的 `getDisplayRate()` 算回饋率——**不另寫一套**，否則遲早跟畫面分岔
（`docs/project/cashback-engine.md` 第 6 節「三處實作必須一致」）。

## 站長已定案（2026-09-17 第四輪）

- **卡片特色的通路名最多兩行**才截斷（單行截掉太多看不懂在講哪個通路），
  順便從列 2 個商家改成列 3 個
- **同一個通路名只留回饋率最高的那一個**（聯邦 LINE Bank 卡的「萊爾富門市」
  原本 10% 與 5% 並列，會被當成資料錯誤）。⚠️ **去重必須在挑前 5 名之前做**，
  否則重複那行會佔掉名額、把真正的第 5 名擠掉；被丟掉的那個若帶 hype 字眼要移轉給留下來的
- **Highlights 反查對到多組活動時取回饋率最高的那組** —— 確認採用
- 中信 LINE Pay 卡那列空活動站長已在 Sheets 修正。
  ⚠️ repo 的 `cards.data` 仍是 `20260913-162712`、還含舊資料，下次 `exportToJSON()` 才會進來

### 「支付寶 4.5% vs 3%」其實是等級問題（站長指出，確認屬實）

第二輪報告的那筆不一致，是當時還在取「第一個級別（簡單選）」算出來的。
改成**取最高級別（UP選）**之後算出來就是 **4.5%**，跟 Highlights 寫的完全一致。

**目前唯一剩下的不一致是中信 uniopen 的「夢時代購物中心」**（sheet 7%／反查到 11%），
依定案取回饋率最高的那組，顯示 11%。

同一張卡因此會看到夢時代出現兩行：`11% 夢時代購物中心〔icashPay + 統一集團踩點任務〕`
與 `7% 統一超商、康是美、夢時代購物中心…等 38 項〔統一集團踩點任務〕`。
那不是重複資料，是同一商家的兩條不同路徑（走不走 icashPay），
標籤不同所以「同名只留最高」抓不到。要合併只能人工指定。

## 站長已定案（2026-09-17 第三輪）

- **每檔活動保留自己的詳情**：適用通路／達成條件／活動期間／新戶定義／備註（備註長就收兩行＋「展開 ▾」）。
  點主卡看主檔的、點堆疊層看那一層的；同一組內一次只開一個
- **同卡多檔活動各自獨立、不相加**（各有不同達成條件）。排序只看「單檔最大值」，畫面上沒有任何加總
- **沒有 `slot22` ＝ 沒有國外消費回饋，整行不顯示**（取代第二輪「退回卡片級欄位推算」）。
  實測 8 張卡只剩「國內一般消費」一行：滙豐 Live+、滙豐匯鑽、玉山 Uni、聯邦 LINE Bank、
  星展 eco、玉山 Pi、遠東快樂卡、富邦 J 卡
- **分級卡取最高級別**。⚠️ 不能用 `levelSettings` 的鍵順序——實測 4 張分級卡有兩種排法
  （玉山 Uni／國泰 CUBE 由低到高，永豐大戶／凱基誠品由高到低）。改成實算：
  每個級別把所有可顯示槽位算一遍，取「最高回饋率」最大的那個。
  結果：永豐大戶卡 → 大戶Plus等級、玉山 Uni 卡 → UP選、凱基誠品 → 黑卡
- **Highlights 保證入列，但跟其他活動一起依回饋率倒序**（固定顯示 ≠ 排最前面）。
  第二輪那個「4.88% 釘在 5.88% 上面」的怪現象因此消失
- **卡片特色一律顯示商家**：標籤永遠取 `items`，不顯示 `category`
- **獎品沒有活動宣傳圖就退回卡片圖**
- **取消獨立的「獎品類」區**——站長指出它跟上方的首刷禮活動重複。確認屬實：
  那是第一版「一檔活動一列」時為了安置沒有現金價的獎品而設的，改成依卡片分組之後
  獎品活動本來就在各自卡片的堆疊裡（中信 uniopen 那 3 檔就在它自己的第 2～4 層），
  再列一次就是同一批資料出現兩次。現在只有 3 張卡完全沒有現金價
  （永豐 Green、富邦 J、兆豐 BT21），自然排在最後

### 一項沒照做，待站長確認

卡片特色的標籤已全部改成商家，但**保留了灰色的條件後綴**
（例：`10% 詹記麻辣火鍋、萬客什鍋…等 34 項〔切換「Chill刷」方案〕`）。
理由：台新那五行是**五個互斥的方案**，一個月只能選一個；拿掉之後五行看起來像可以
同時拿 10%＋5%＋3.8%＋3.8%。要拿掉的話一行就改掉。

## 站長已定案（2026-09-17 第二輪）

- **版型＝卡圖前導緊湊列**；堆疊用**厚堆疊**（金額＋單位一行、summary 一行）
- **卡名放最上方獨立一行**（量測結論見下）
- **卡片特色預設收合**，點主卡右下角的「卡片特色」按鈕才展開
- **卡片特色不可以用堆疊法**——堆疊是「卡疊卡」（白底、卡片陰影、逐層內縮），
  特色是**從主卡拉出來的抽屜**（淺藍底、有邊框、無卡片陰影），兩種形狀刻意不同
- **兩者互斥**：展開特色時其他活動整疊收起，收回特色它們才回來。同一個位置只會有一種東西
- **卡片用途沒填就整行不出現**（同匯出端既有的 `addOptionalField` 慣例，舊資料相容，可以慢慢填）

### 卡名放哪：量測結論

`measure-card-names.js` 實測 Noto Sans TC 700、22 個卡名全量：

| 卡名 | 10px | 11px | 12px |
|---|---:|---:|---:|
| 中信 Uniopen 聯名卡 | 104 | 114 | 125 |
| 聯邦 LINE Bank 卡 | 94 | 104 | 113 |
| 滙豐現金回饋御璽卡 | 90 | 99 | 108 |

卡圖維持 78px 寬時，**11px 有 6/22 個卡名放不下**。要一行不截斷只有兩條路：
卡圖欄加寬到 **118px**（右欄從 217px 縮到 177px，獎勵大字被擠小），
或**把卡名移到最上方**（全寬 306px，任何卡名都放得下）。**預設取後者**——
獎勵大字是這次改版的全部重點，不該為了卡名讓位。兩種都做進互動原型的控制台了。

## 站長已定案（2026-09-17 第一輪）

**版型共同前提**

- 不編號，依「最高可拿」倒序
- 同卡多檔活動**堆疊**：上緣方角、只留下方兩個圓角，往上塞進主卡底下
- 刷卡金類**直接顯示 summary**，最多兩行、截斷不補救
- 獎品區以獎品為主：**縮圖 ＋ 完整品名不截斷**（`gift_image_url` 有圖的 7 檔走現成的
  `.promo-gift-thumb` 縮圖＋lightbox 機制，無圖的沿用 `onerror` 隱藏）
- 淘汰：長條圖、金額級距、一卡一列比較表、類型分頁

**卡片特色區塊**

- 區塊名稱：常態活動 → **「卡片特色」**
- 右上角「查看全部 ›」（原卡名旁的 ⓘ 移到這裡），點擊直接捲到詳情頁的**指定通路回饋**
- 資料來源走 **Highlights 必列 ＋ 自動補滿**
- **固定顯示國內消費／國外消費的回饋率**
- 台新 Richart 卡不用 `category`、改用 `items`
- 「全場最高」不用 chip（會擠爆版面），改用其他呈現方式

## 新的實作結論（v2 實跑）

**標籤規則（已一般化，不是台新特例）**：`category` 命中
`/方案|切換|任務|首次|綁定|登錄|滿額|滿千/` 就判定為「條件」而非「通路」→ 標籤改用
`items`、原 `category` 降級成灰色條件後綴。台新 Richart 卡因此從
「10% 切換「Chill刷」方案」變成「10% 詹記麻辣火鍋、萬客什鍋…等 34 項〔切換「Chill刷」方案〕」。

**固定兩行直接讀骨幹槽 slot 21／22**（見 `cashbackmodel-fill-guide.md` 第 4 節），
跑同一支 `getDisplayRate()`。覆蓋率實測：**slot21 = 23/23、slot22 = 15/23**；
缺的 8 張退回卡片級欄位推算（那條路沒有 `cashbackModel` 把關，數字沒有保證）。

**Highlights 反查**：`merchant` 可能是快捷搜尋 displayName（「所有計程車」），
必須走快捷展開再比對 `items` 全清單。一卡一通路可能命中多組活動，
目前取**回饋率最高**的那組。實測兩則因此與 sheet 對不上：

- 玉山 Uni 卡「支付寶」：sheet 4.5% vs 真活動 3%
- 中信 uniopen「夢時代購物中心」：sheet 7% vs 真活動 11%

⚠️ **折進固定兩行的條件**：只有「反查不到任何指定通路槽」時才把 Highlights 折進
國內／國外那兩行。一開始寫成用字串比對 merchant 名稱，結果中信 uniopen 的
「國外實體消費」（對得到 11% 的加碼槽）被折進 3% 的固定行、直接把數字講錯了。

**釘選的副作用**：釘選項不見得是回饋率最高的。滙豐 Live+ 卡會出現
「4.88%（釘選）排在 5.88% 上面」——人工策展本來就會這樣，但要先接受
「這一欄不是嚴格遞減」，否則日後會被當成排序 bug 回報。

## 待站長裁決

1. ~~Highlights 反查對不上時怎麼辦？~~ → 第四輪定案：**取回饋率最高的那組**，以真活動為準。
2. ~~分級卡顯示哪一級？~~ → 第三輪定案：**取最高級別**（實算，不看鍵順序），標題旁標「以『○○』計」。
3. ~~缺 slot22 的 8 張卡要不要補？~~ → 第三輪定案：沒有就是沒有，整行不顯示。
4. ~~同一張卡的多檔新戶活動能不能相加？~~ → 第三輪定案：**不能相加，各自獨立**。
5. **generatePromosPageHtml 需要 `getDisplayRate()`，而它在 `js/`、Apps Script 讀不到。**
   建議照 `tools/build-merchant-pages.js` 的成例在 Cloudflare Pages build 時用 Node vm 注入，
   不要把 `getDisplayRate()` 移植進 `cards-export.gs`（那會變成第四份副本）。

## 「查看全部」怎麼接（不用新做東西）

詳情頁已有 `#card-special-section` 與對應導覽鈕
（`index.html` 的 `.card-detail-nav-btn[data-section="card-special-section"]`）；
promos 頁已有常駐 iframe ＋ `postMessage({type:'pmc-open-card', cardId})`（`promos.js`）。
加一個 `section` 欄位、embed 端收到後點一下那顆導覽鈕即可——**兩邊各一行**。

## 資料問題（與改版無關，但會顯示在頁面上）

`newCardholderPromos` 有一列**中信 LINE Pay 卡**的活動：`promo_types`、獎勵欄位、
`period_end` 全空。無 `period_end` ＝不限期，所以沒被過期過濾擋掉，
現在就渲染在 `/promos` 上，是一張什麼都沒寫的空卡片。要嘛補完、要嘛刪列——
生成器不該替資料做這個決定。

## 桌機多活動卡版面：第二輪修正 ＋ 三個提案（2026-09-18）

檔案：`desktop-multiact-2026-09-18.html`（開瀏覽器即可，無依賴；1440px 觀看）

站長對第一版桌機跨 2 欄的回饋：

- 「向右再向下延伸的設計也很亂」
- 「向下延伸的活動的卡片不要越縮越小」
- 「第二個活動向右延伸的設計改為用向右的箭頭代替，其寬度要跟第三、第四個活動一致」

**已實作（現況那一區）**：右半格改成等寬等大的獨立活動卡＋欄間一支向右箭頭。
逐層內縮與「第 2 檔從主卡後方延伸」的方角處理全部移除。

**另外三個提案**（頁面上依序排列，各附優缺點）：

| 方案 | 做法 | 一句話 |
|---|---|---|
| A 條列式 | 右欄改一行一檔：金額對齊成一欄＋一句話，點行就地展開 | 最省高度、金額可互比；但其他活動的「卡片感」沒了，獎品圖沒地方放 |
| B 單框分欄 | 整組只有一個外框，左右用直線分欄、右欄活動用橫線分隔，底部一條「同一張卡・N 檔活動可同時參加」窄帶 | 最單純、零卡中卡；但左右份量接近，主活動不再明顯是主角 |
| C 右欄雙格 | 其他活動排成 2 欄小格，3~4 檔只佔兩列 | 整組最矮、列內空白最少；但小格寬度剩一半，長文案一定截斷、奇數檔會缺一格 |

頁面最後一區用**永豐幣倍卡**（文案最長的多活動卡）對照 A 與 C 的截斷風險。

## 「卡名橫跨兩欄」看起來怪 → 四種群組呈現法（2026-09-20）

檔案：`group-header-patterns-2026-09-20.html`（1440px 觀看）

站長回報：09-20 做出來的「卡名橫跨兩欄」確實符合要求，但看起來怪。診斷出兩個具體原因：

1. **白色橫幅裡只有一行短字，其餘約七成是空的**——它自己不像一個有內容的區塊，
   比較像一條沒收好的邊。
2. **橫幅底下掛著兩根長度差很多的柱子**（主活動 370px、右邊那疊 490px），
   像表格標題底下接了參差不齊的內容。

換句話說，問題不在「橫跨兩欄」這個結構，而在「橫幅是張什麼都沒裝的卡」。
四種業界做法（都套上 iLEO 4 檔／永豐幣倍卡 3 檔／滙豐 Live+ 1 檔的真資料）：

| 做法 | 參考 | 一句話 |
|---|---|---|
| **A 左側品牌欄** | Booking.com「一間飯店、多種房型」、MoneySuperMarket／Compare the Market 的商品列 | 卡圖＋卡名＋申辦鈕收進固定寬度左欄，右欄整欄是活動清單。身分在左、選項在右 |
| **B 群組標題落在背景上** | Gmail 日期分段、Linear／Notion 的 group header、App Store 分區標題 | 卡名改成灰底上的小標題，白色橫幅整條消失。改動最小 |
| **C 整組收進同一個外框** | Apple／Stripe 方案比較表、Google Flights 展開後的票價區 | 保留現有結構，只是把外框補上，讓標題列變成容器的一部分 |
| **D 主活動是卡、其他降級成附屬列** | Amazon「Other sellers on Amazon」、Google Shopping 其他賣家 | 卡名留在主卡裡（跟單檔卡一模一樣），其他活動變成淺灰緊湊清單 |

**建議 A**：卡名永遠跟卡圖綁在一起、不會落單；兩欄長度不一致不再是問題（左右關係而非上下關係）；
申辦鈕固定在左欄，滑到第幾檔活動都看得到。代價是單檔卡（373px 寬）擺不下左右欄，
只能維持直式，兩種卡骨架會不同。
**要最小改動就選 C**（結構不動，補外框）；**要順便解決空洞問題就選 D**（附屬列很矮，多檔卡高度接近單檔卡）。
