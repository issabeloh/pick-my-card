# Cloudflare Pages 回應標頭設定（2026-09-02 新增）
#
# 這個檔由 Cloudflare Pages 讀取（必須放在部署輸出根目錄＝repo 根目錄），
# 部署後對每個回應加上下面的安全標頭。改完不用改任何 HTML。
# 語法：路徑樣式一行，接著縮排兩空格的 `標頭名: 值`。
#
# ⚠️ 改這個檔前先看清楚每一條的註解。這裡有兩條「不是改完就算數」的標頭：
#    CSP 拆成兩條：強制版只鎖三條零風險指令；完整的嚴格版仍在 -Report-Only 觀察
#    （白名單漏一個就整站壞）。兩者的分工與搬遷條件寫在該處註解。
#    HSTS 已上到 max-age=31536000（一年），日後新增瀏覽器用的子網域前必讀該條註解。

/*
  # 禁止本站頁面被別的網域用 iframe 嵌入（防點擊劫持／冒名嵌站）。
  # 同網域自己嵌自己仍可以，所以不影響站內任何功能。
  X-Frame-Options: SAMEORIGIN

  # 明確關掉本站永遠用不到的瀏覽器功能。沒列出來的功能維持瀏覽器預設，
  # 所以 GA／Clarity／AdSense 用到的 attribution-reporting、browsing-topics 等不受影響。
  Permissions-Policy: camera=(), microphone=(), geolocation=(), payment=(), usb=()

  # 本站資源（頁面、卡片圖、cards.data…）只給本站自己載入，不給別的網站盜連嵌入。
  # 只影響瀏覽器端的跨站嵌入；社群平台抓 og:image 是伺服器端抓取，不受影響。
  Cross-Origin-Resource-Policy: same-origin

  # 切斷「別的網站用 window.open 開了本站之後，還一直握著本站視窗參照」這條線。
  # 擋掉的實際攻擊：反向分頁劫持（使用者切走後，那個站在背後把本站分頁換成
  # 長得一樣的假登入頁）、以視窗屬性做的狀態側信道探測。
  #
  # 為什麼是 -allow-popups 而不是 same-origin：本站的 Google 登入走
  # signInWithPopup，是「本站開別人」。allow-popups 保留這個方向的關係，
  # 只切斷「別人開本站」。改成 same-origin 會弄壞登入，不要改。
  Cross-Origin-Opener-Policy: same-origin-allow-popups

  # ---- HSTS：已上到一年（2026-09-02 當天完成階梯）----
  # 作用：在使用者瀏覽器留一張「這個網域只走 https」的字條，消滅「網址列直接打
  # pickmycard.app」時那一次明文 http 請求的中間人空窗。字條的倒數在每次來訪時重置，
  # 所以只要使用者一年內回訪過就不會過期；伺服器端設定一次即可，不用續約。
  #
  # 為什麼敢直接上 31536000（原本從 max-age=300 起步）：站長 2026-09-02 提供 Cloudflare
  # DNS 全表，確認 *.pickmycard.app 底下**沒有任何會被瀏覽器以 HTTP 開啟的子網域**——
  # 當時的子網域紀錄只有 _dmarc（DMARC 政策）、send（Amazon SES 的 MX/SPF）與兩筆
  # _domainkey（DKIM），全都是郵件與驗證用途，走 SMTP/DNS 不走 HTTP，includeSubDomains
  # 碰不到它們（www 是同日稍晚才加的，見下）。主網域 pickmycard.app 是 Proxied CNAME → pick-my-card.pages.dev，
  # 憑證由 Cloudflare 自動續。既然沒有要觀察的對象，階梯就沒有意義。
  #
  # 同日稍晚新增了 www：CNAME www → pickmycard.app（**Proxied**，所以 Universal SSL
  # 自動涵蓋、符合下面那條規則），並在 Cloudflare 的 Redirect Rules 加一條
  # 「https://www.pickmycard.app/* → https://pickmycard.app/${1}」301（保留 query string）。
  # 轉址在邊緣就完成，請求不會送到 Pages，所以 www 不必登記成 Pages 的自訂網域。
  # 這兩項都是 Cloudflare 後台設定、不在 repo 裡——在這裡留一筆是因為它們與下面那條
  # 規則直接相關，改動時要一起想。
  #
  # ⚠️ 日後新增任何「用瀏覽器開的」子網域（blog.、api. 之類），它**必須從第一天
  #    就有有效 HTTPS**——includeSubDomains 已經在使用者瀏覽器裡生效，沒有 https 的
  #    新子網域會直接連不上，而且你不會收到任何回報。掛在 Cloudflare 上（Proxied）
  #    就自動有憑證，沒問題；掛在別處的話先確認憑證再上線。
  #
  # ⚠️ 要反悔：把 max-age 改成 0 並保持部署，等回訪者的字條被清掉（最長一年）。
  #    所以網域若有轉手可能，提前一兩個月改成 0 就能乾淨交接。
  #
  # ⚠️ 絕對不要加 preload 參數、也不要去 hstspreload.org 提交網域。
  #    那份名單是烤進 Chrome 的，要移除得申請並等好幾個月——那才是真正不可逆的東西，
  #    上面那條「改成 0 就能退場」的路對 preload 名單無效。
  Strict-Transport-Security: max-age=31536000; includeSubDomains

  # ---- CSP 之一：強制版（2026-09-09 起真的在攔）----
  # 為什麼要拆成兩個標頭：CSP 是全有全無的，一旦把下面那條的 -Report-Only 拿掉，
  # 整套規則同時生效——而 script-src/connect-src/frame-src 那串廣告網域是移動目標
  # （Google 隨時冒出新的兄弟網域），等於「等到完全確定」這天可能永遠不會來，
  # 結果就是一條防護都沒生效。所以把規則拆成兩份各走各的：
  #   這一條（強制）只放三條「本站根本沒在用、廣告也碰不到」的指令，現在就上鎖；
  #   下面那條（只回報）維持完整的嚴格版，繼續觀察 script-src 那串。
  #
  # 這三條為什麼零風險（動之前先確認前提還成立）：
  #   object-src 'none'      擋 <object>/<embed> 老式外掛嵌入——全 repo grep 過，零使用
  #   base-uri 'self'        擋動態插入 <base> 竄改全站連結目標。
  #                          ⚠️ 商家頁 merchant/*.html **有** <base href="/">（生成器產的，
  #                          整頁的 css/js/圖片相對路徑都靠它）。`/` 解析後是同源，
  #                          符合 'self' 所以照常運作——2026-09-09 用本機送強制版標頭
  #                          實測過：base 正常解析、4 個 stylesheet 全載入、零違規。
  #                          但日後若把 base 的 href 改成別的網域就會被擋（那本來也該擋）。
  #   frame-ancestors 'self' 擋被別站 iframe 嵌入——與上面的 X-Frame-Options 同一件事，
  #                          兩者並存是刻意的（舊瀏覽器只認 X-Frame-Options）
  # 刻意**不放** form-action：本站的表單行為都走 JS 不走 form submit，但廣告若在
  # 繼承本頁 CSP 的 iframe（about:blank/srcdoc）裡送 form 就會被擋。它留在下面那條
  # 只回報版裡繼續觀察，確認沒有違規再考慮搬上來。
  #
  # ⚠️ 這裡刻意沒有 default-src：沒列出的資源類型完全不受這條限制，
  #    才不會在白名單還沒驗完的情況下誤擋。要加指令請先在下面那條觀察乾淨再搬上來。
  Content-Security-Policy: object-src 'none'; base-uri 'self'; frame-ancestors 'self'

  # ---- CSP 之二：只回報、不攔截（2026-09-02 起觀察中）----
  # ⚠️ 標頭名稱結尾的 -Report-Only 是關鍵：違規只會印在瀏覽器 Console，
  #    東西照常載入，線上使用者完全不受影響。確認 Console 乾淨之前，
  #    絕對不要把 -Report-Only 拿掉。
  #
  # 觀察方法：部署後打開 F12 Console，正常走一輪
  #   首頁搜尋 → 選卡／持有卡 → 卡片詳情 → 登入／登出 → FAQ → 新戶活動 → 隱私權 → 使用須知 → 商家頁
  # 看到 "Refused to load ... violates the following Content Security Policy directive"
  # 就把那個網域補進對應的 directive，重新部署再看一輪。
  #
  # 白名單怎麼來的（改動前先確認你要加的東西屬於哪一類）：
  #   www.gstatic.com          Firebase SDK（index.html/faq.html 的 modulepreload + import）
  #   googletagmanager / google-analytics   GA4
  #   *.clarity.ms             Microsoft Clarity（www.clarity.ms 是 loader，
  #                            它會再去 scripts.clarity.ms 抓本體——所以用萬用字元）
  #   *.cloudflareinsights.com Cloudflare Web Analytics 的 beacon。這支不是我們掛的，
  #                            是 Cloudflare 在邊緣自動注入的，所以 repo 裡搜不到
  #   AdSense 一整組（*.googlesyndication.com / *.doubleclick.net /
  #                            *.googleadservices.com / *.adtrafficquality.google /
  #                            adservice.google.com / fundingchoicesmessages.google.com）
  #                            ⚠️ 刻意用萬用字元而非逐一列舉：廣告網域是移動目標，
  #                            Google 隨時會冒出新的兄弟網域（2026-09-09 就為了
  #                            ep2.adtrafficquality.google 的 iframe 改過一次）。
  #                            逐一列舉＝每次都要再來一輪 Console 觀察。
  #   fonts.googleapis.com（樣式）＋ fonts.gstatic.com（字型檔）   Noto Sans TC
  #   *.googleapis.com（connect）  Firestore／Auth／Storage 的 API 呼叫
  #   pick-my-card-28f2a.firebaseapp.com（frame）  Firebase 登入 popup
  #
  # 刻意先不寫 'unsafe-eval'：想從觀察結果確認廣告腳本到底需不需要它，
  # 而不是先給了就永遠拿不掉。Console 若出現 eval 相關違規再補。
  #
  # 'unsafe-inline' 則是拿不掉的：本站沒有 build 步驟，index.html 等頁面有大量
  # 內聯 <script> 與 style="" 屬性，改用 nonce 得先有打包流程。即使如此，
  # CSP 仍然把「可以載入程式的網域」鎖死在上面這份清單，這才是主要價值。
  #
  # 沒有設 report-uri／report-to：本站沒有收集違規回報的後端，
  # 違規只看瀏覽器 Console 就好，不要為了這個去接第三方服務。
  Content-Security-Policy-Report-Only: default-src 'self'; base-uri 'self'; object-src 'none'; frame-ancestors 'self'; form-action 'self'; script-src 'self' 'unsafe-inline' https://www.gstatic.com https://www.googletagmanager.com https://www.google-analytics.com https://*.clarity.ms https://static.cloudflareinsights.com https://*.googlesyndication.com https://*.doubleclick.net https://*.googleadservices.com https://adservice.google.com https://*.adtrafficquality.google https://fundingchoicesmessages.google.com https://apis.google.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' data: https://fonts.gstatic.com; img-src 'self' data: blob: https:; media-src 'self'; connect-src 'self' https://cloudflareinsights.com https://static.cloudflareinsights.com https://*.googleapis.com https://*.google-analytics.com https://*.analytics.google.com https://*.googletagmanager.com https://*.clarity.ms https://*.firebaseio.com wss://*.firebaseio.com https://*.doubleclick.net https://*.googlesyndication.com https://*.googleadservices.com https://*.adtrafficquality.google https://adservice.google.com https://fundingchoicesmessages.google.com https://*.gstatic.com https://www.google.com; frame-src 'self' https://pick-my-card-28f2a.firebaseapp.com https://accounts.google.com https://*.google.com https://*.doubleclick.net https://*.googlesyndication.com https://*.googleadservices.com https://*.adtrafficquality.google https://fundingchoicesmessages.google.com https://*.gstatic.com; worker-src 'self' blob:; manifest-src 'self'
