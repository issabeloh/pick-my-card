{
  "name": "pick-my-card-functions",
  "description": "Cloud Functions for Pick My Card — feedback 即時通知",
  "private": true,
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "logs": "firebase functions:log --only notifyOnFeedback",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.0.2",
    "firebase-functions": "^6.3.0",
    "nodemailer": "^6.9.16"
  }
}
